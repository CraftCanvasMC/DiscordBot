{
  "name": "download",
  "description": "Get latest CanvasMC builds",
  "options": [
    {
      "name": "project",
      "description": "Project to fetch builds for",
      "type": 3,
      "required": false,
      "choices": [
        {
          "name": "Canvas",
          "value": "canvas"
        },
        {
          "name": "Horizon",
          "value": "horizon"
        }
      ]
    },
    {
      "name": "channel",
      "description": "Optional channel version filter (example: 1.21.11)",
      "type": 3,
      "required": false
    }
  ]
}

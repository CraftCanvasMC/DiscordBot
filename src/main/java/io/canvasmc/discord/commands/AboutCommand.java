package io.canvasmc.discord.commands;

import dev.jsinco.discord.framework.commands.DiscordCommand;

@DiscordCommand(name = "about", description = "Get information about a project")
public class AboutCommand extends ProjectCommand {
    // alias
}
